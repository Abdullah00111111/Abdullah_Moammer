package HomeWorkTow;

public class Q_Six {
    public static void main(String[] args) {
        
        // String s = "Hello", t = "World";

        // s.length() + t.length();                 = 10
        // s.substring(1, 2);                       = e
        // s.substring(s.length() / 2, s.length()); = llo 
        // s + t                                    = HelloWorld
        // t + s                                    = WorldHello
    }
}
